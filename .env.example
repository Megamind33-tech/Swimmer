# Environment variables for Swimmer app

# APP_URL: The URL where this app is hosted (optional)
# Used for self-referential links, OAuth callbacks, and API endpoints
# APP_URL="http://localhost:3000"
